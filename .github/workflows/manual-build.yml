name: Manual Build Trigger

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build'
        required: true
        default: '1.0.0'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build triggered for version ${{ github.event.inputs.version }}"
